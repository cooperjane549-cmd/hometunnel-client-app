name: hometunnel_client
description: "HomeTunnel Client Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  
dependencies:
  http: ^1.2.0
  cryptography: ^2.7.0
  wireguard_flutter: ^0.3.0   # check pub.dev for the current version
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
